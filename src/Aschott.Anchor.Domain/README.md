# Aschott.Anchor.Domain

Building block for DDD primitives: `Entity<TKey>`, `AggregateRoot<TKey>`, `ValueObject`, `MultiTenantEntity<TKey>`, `DomainEvent`. Zero dependencies on infrastructure or framework concerns.
